<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:microcharts="clr-namespace:Microcharts.Maui;assembly=Microcharts.Maui"
             x:Class="Mauixui.Views.FinanceView"
             BackgroundColor="#1E1E1E">

    <Grid x:Name="RootGrid">

        <!-- Главная страница -->
        <ScrollView x:Name="FinanceHome" Padding="20">
            <VerticalStackLayout Spacing="15">

                <Label Text="💰 Финансы" FontSize="26" TextColor="White" FontAttributes="Bold"/>
                <Label x:Name="BalanceLabel" Text="Баланс: 0 Br" FontSize="20" TextColor="#23D160"/>

                <HorizontalStackLayout Spacing="10">
                    <Button Text="📁 Категории" BackgroundColor="#40444B" TextColor="White" Clicked="OpenCategories"/>
                    <Button Text="💼 Бюджеты" BackgroundColor="#40444B" TextColor="White" Clicked="OpenBudgets"/>
                    <Button Text="🏦 Активы" BackgroundColor="#40444B" TextColor="White" Clicked="OpenAssets"/>
                    <Button Text="📊 Статистика" BackgroundColor="#40444B" TextColor="White" Clicked="OpenStatistics"/>
                </HorizontalStackLayout>

                <!-- Добавление операции -->
                <Frame BackgroundColor="#2D2D30" CornerRadius="16" Padding="12" Margin="0,10,0,0">
                    <VerticalStackLayout Spacing="10">

                        <HorizontalStackLayout Spacing="10">
                            <Picker x:Name="TypePicker" WidthRequest="150" Title="Тип">
                                <Picker.Items>
                                    <x:String>Доход</x:String>
                                    <x:String>Расход</x:String>
                                </Picker.Items>
                            </Picker>

                            <Picker x:Name="CategoryPicker" WidthRequest="150" Title="Категория"/>
                        </HorizontalStackLayout>

                        <Entry x:Name="DescriptionEntry" Placeholder="Описание"
                               BackgroundColor="#40444B" TextColor="White" PlaceholderColor="#888888"/>

                        <Entry x:Name="AmountEntry" Placeholder="Сумма" Keyboard="Numeric"
                               BackgroundColor="#40444B" TextColor="White" PlaceholderColor="#888888"/>

                        <DatePicker x:Name="DatePicker" Format="dd.MM.yyyy" TextColor="White"/>

                        <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
                            <Button Text="Добавить" BackgroundColor="#4CAF50" TextColor="White" Clicked="AddFinanceItem"/>
                            <Button Text="Очистить" BackgroundColor="#FF4B4B" TextColor="White" Clicked="ClearFields"/>
                        </HorizontalStackLayout>

                    </VerticalStackLayout>
                </Frame>

                <Label Text="История операций" TextColor="White" FontAttributes="Bold" FontSize="16"/>

                <!-- История (ПРАВИЛЬНО) -->
                <ScrollView HeightRequest="250">
                    <VerticalStackLayout x:Name="FinanceList" Spacing="6"/>
                </ScrollView>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Внутренние экраны -->
        <Grid x:Name="InnerViewContainer"
              BackgroundColor="#1E1E1E"
              IsVisible="False">

            <VerticalStackLayout>

                <Button Text="← Назад" 
                        BackgroundColor="#40444B" 
                        TextColor="White"
                        Margin="10" 
                        Clicked="GoBackToFinance" 
                        WidthRequest="100"
                        HorizontalOptions="Start"/>

                <ContentView x:Name="InnerViewContent"/>
            </VerticalStackLayout>

        </Grid>

    </Grid>

</ContentView>
